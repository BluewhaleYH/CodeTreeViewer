import './index.css'

const root = document.getElementById('app')

if (root) {
  root.innerHTML = `
    <main class="shell">
      <h1>CodeTreeViewer</h1>
      <p>스캐폴딩이 정상 동작합니다. (M0)</p>
    </main>
  `
}
