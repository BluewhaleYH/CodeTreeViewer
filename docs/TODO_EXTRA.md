# TODO_EXTRA — 한계 · 미해결 · 기술부채 모음

> 구현 중 발견한 **한계점 · 임시 결정 · 미뤄둔 문제**를 모은다. **모든 MVP 작업이 끝난 뒤 다시 훑어볼 목록.**
> - 정식 기능 투두 → `docs/TODO.md`
> - SPEC 단계의 미해결/제안 → 각 SPEC 문서 하단 `추가)`
> - 본 문서는 그 사이의 "구현하다 생긴 빚/한계"를 추적한다.
>
> 각 항목 표기: **[현황]** 무엇이 부족한가 · **[영향]** 무엇이 위험한가 · **[대응]** 예정 마일스톤 또는 `미정`. (발생: 작업 단위)
>
> **🚧 보류**: 현재 환경/결정상 지금 해결할 수 없어 의도적으로 미루는 항목(빈 체크박스로 두되 사유를 명시). 2026-06-21 일괄 검토에서 다음 5건을 보류로 합의: tree-sitter-kotlin 오탐 · web-tree-sitter 0.20.x 고정(둘 다 최신 문법 wasm 자체 빌드 필요) · 분석 워커 스레드(D11 수용) · Kotlin 의미 구분(현 범위 충분) · `allowScripts`(현 환경 설치 의존). 그 외 빈 체크박스는 해소 대상.

## A. 보안 / 배포

- [x] **렌더러 CSP 완화** (발생: M0) — **해결**(#62): 정적 `<meta>` CSP 제거, main에서 응답 헤더로 dev/prod 분기 주입(`src/main/security.ts`). prod는 `script-src 'self'`로 `unsafe-inline`/`unsafe-eval` 제거. prod 빌드에서 인라인 스크립트 차단 양성 검증, Monaco/cytoscape/로그뷰 위반 0건.

- [x] **패키징 wasm 위치** (발생: M3_2/M3_3) — **M9 해결**: `electron-builder.yml` `extraResources`로 wasm 5종(tree-sitter + java/kotlin/c/cpp)을 asar 밖 `resources/wasm/`에 복사, 런타임 `wasm-paths.ts`가 패키징 경로를 해석.

- [x] **앱 아이콘 미설정** (발생: M0) — **해결**(#76): 관계도 모티프 SVG → `resources/icon.png`(1024²), electron-builder `icon` 지정으로 linux png/win ico 자동 생성. CI Windows nsis로 ico 변환 검증.

- [ ] 🚧 **`package.json`의 `allowScripts` 필드** (발생: M0) — **보류**(2026-06-21 합의)
  - [현황] 이 샌드박스 npm 래퍼 전용 필드(electron/esbuild 설치 스크립트 승인).
  - [영향] 표준 npm/CI는 무시(무해)하나 저장소에 환경 특화 흔적이 남음.
  - [보류 사유] 현 개발 환경의 의존성 설치가 이 필드에 의존 → 지금 제거하면 electron/esbuild 설치가 깨짐. CI/표준 환경 정비 시 제거 검토.

## B. 파싱 / 분석 정확성

- [ ] 🚧 **tree-sitter-kotlin 0.3.1 오탐** (발생: M3_2) — **보류**(현재 해결 불가, 외부 의존)
  - [현황] `package` + `class` 조합 등 일부 유효 Kotlin 코드에서 ERROR 노드 발생. 실제 안드로이드 Kotlin 파일은 package 선언이 흔함.
  - [영향] `hasError()`가 자주 true. 추출 정확도/신뢰 표시에 영향.
  - [완화 적용됨] M4 추출은 `hasError` 여부와 무관하게 노드 쿼리로 수행(오탐의 실해 최소화).
  - [보류 사유] 근본 해결은 최신 Kotlin 문법 wasm 자체 빌드 필요. web-tree-sitter 0.20.x ABI 핀에 묶여 현 환경에서 불가(메모리 `tree-sitter-wasm-versions`).

- [ ] 🚧 **web-tree-sitter 버전 고정(0.20.8)** (발생: M3_2) — **보류**(현재 해결 불가, 외부 의존)
  - [현황] 문법 wasm(`tree-sitter-wasms` 0.1.13, CLI 0.20.x)과 ABI를 맞추려 런타임을 0.20.8로 고정.
  - [영향] 런타임만 업그레이드하면 `Language.load` 실패. 최신 문법/기능 수용 어려움(기술부채).
  - [보류 사유] 문법 wasm을 최신 CLI로 자체 빌드해야 동반 업그레이드 가능. tree-sitter-kotlin 오탐과 동일 뿌리. 현 환경에서 빌드 툴체인 부재로 불가(메모리 `tree-sitter-wasm-versions`).

- [x] **`ANALYZER_VERSION` 수동 관리** (발생: M3_4) — **해결**(#67): 빌드 시 `src/main/analysis` 소스 해시를 `__ANALYZER_HASH__`로 주입(electron.vite define) → 분석 로직 변경 시 캐시 자동 무효화. 수동 상향 불필요.

- [x] **캐시 무효화 키 정밀도(stat 기반)** (발생: M3_4) — **해결**(#67): 하이브리드 지문 — stat(mtime+size) 빠른 경로 + stat 변경 시 내용 해시 재확인(mtime만 바뀐 경우 재분석 생략).

## C. 성능 / 확장성

- [ ] 🚧 **분석이 워커 스레드 아님** (발생: M3_3) — **보류**(결정 D11 수용)
  - [현황] 메인 프로세스에서 파일 단위 협력적 양보(`setImmediate`)로 비차단. 단일 대형 파일 파싱 중에는 그 호출 동안 블록.
  - [영향] 초대형 파일/프로젝트에서 일시적 응답성 저하 가능.
  - [보류 사유] **D11에서 메인+양보 유지를 결정**했고 워커는 추후 대규모 성능 최적화로 수용. 진짜 워커 스레드/`utilityProcess` 분리는 그때 진행.

- [x] **파스 트리 메모리 보관** (발생: M3_3) — **해결**(#66): `analyzeScanned`/`reanalyzeFile`에서 추출 직후 `tree.delete()`로 WASM 파스 트리 즉시 해제(finally 보장).

- [x] **캐시 디렉터리 정리 정책 부재** (발생: M3_4) — **해결**(#67): `set` 시 캐시 항목이 상한(50)을 넘으면 mtime 기준 오래된 것부터 정리(LRU).

- [x] **대규모 렌더링 성능 합격선 미정** (발생: 계획) — **해결**: `03 §8`에 합격선 확정(초기 ≤2000 노드, 경계에서 팬/줌 ≥30fps). 헤드리스 측정으로 2000 경계 타당성 확인.

- [x] **로그 디스크 스트리밍 미구현** (발생: M11_2) — **해결**(#73): 50MB 초과 로그는 디스크 스트리밍(main 라인 인덱스 + 윈도우 읽기 + 줄 단위 필터/검색/관련 스캔). 작은 파일은 메모리 모드 유지.

- [x] **Monaco 번들 비대** (발생: M12_1) — **해결**(#69): `editor.api` + java/kotlin basic-languages만 선택 import. 빌드 청크 30+개 → 7개(미사용 언어/언어서비스 제거). 코어 ~8MB는 본질적.

- [x] **로그 줄바꿈(wrap) 토글 제거** (발생: M11_2) — **해결**(#72): 등폭 글꼴 기반 가변 높이 가상 스크롤(prefix 누적 오프셋)로 wrap 토글 복원. (스트림 모드는 미지원)

## D. UX 미구현 / 약식

- [x] **닫은 탭 복원(최근 닫은 탭)** (발생: M2) — **해결**(#70): 닫은 프로젝트 탭 이력(최대 10, session.json 영속) + Ctrl+Shift+T LIFO 복원.
- [x] **동일 프로젝트 중복 탭 허용 여부** (발생: M2) — **해결**(#70): 동일 경로 탭이 있으면 새 탭 대신 기존 탭 포커스.
- [x] **단축키 표 확정** (발생: M1) — **해결**(#71): `01 §8`에 전체 표 확정 + Ctrl+F/Ctrl+1~9/Ctrl+Shift+G 추가.
- [x] **경로 깨진(이동/삭제) 탭 처리** (발생: M2) — **해결**(#70): 복원 시 `project:exists` 확인 → 없으면 자동분석 스킵 + 안내 표시.
- [x] **창/탭/파일/뷰 모드 세션 저장·복원** (발생: M1/M2) — **M8 해결**(저장소·창/탭/뷰 복원·손상 대비).
- [x] **뷰 모드 토글 UI** (발생: M2_4) — **M5/M6 해결**(관계도↔트리 전환 UI).
- [x] **스캔 제외 규칙 설정 UI** (발생: M3_1) — **해결**(#74): 설정 모달(칩 추가/삭제 + 기본값) + `settings.json` 영속, 저장 시 재분석.

## E. 검증 / 테스트 인프라

- [x] **네이티브 다이얼로그/메뉴 E2E 미검증** (발생: M1_3/M2/M3_3) — **해결**(#75): Playwright `_electron` E2E 도입. main에서 dialog 스텁 + 메뉴 채널 전송으로 "메뉴→다이얼로그→분석→렌더" 라운드트립 검증. CI에 E2E 잡(xvfb).

- [x] **CI 부재** (발생: 전반) — **M9 해결**: `.github/workflows/build.yml`(Linux/Windows 매트릭스). PR/푸시는 typecheck+lint+test+build, 태그(`v*`)는 멀티 OS 패키징 + GitHub Releases 퍼블리시(`--publish always`).

- [x] **캡처 모드 데모 시드 코드** (발생: M2~M3) — **해결**(#68): 시드 로직+데이터를 `capture-seed.ts`로 추출, `isCapture`일 때만 동적 import → 메인 번들에서 code-split 제외(검수 모드만 로드).

## F. 미결정 SPEC 항목(단계별 결정 예정 — 참고용)

- [x] **영역(Domain) 분류 방식** — **[결정 D10]** 특정 레이아웃 미가정, 모듈/최상위 디렉터리 기반 자동 그룹 + 사용자 매핑(범용). 구체 그룹/색 규칙은 M4_5.
- [x] **파일 의존성 정의 범위** — **[결정 D9]** MVP는 import 기반, 프로젝트 밖 import는 `external` 노드(토글). 동일 패키지/리소스는 MVP 범위 밖.
- [ ] 🚧 **Kotlin 특유 요소(확장 함수/최상위/컴패니언) 처리** — `02` 추가-3. **보류**(2026-06-21 합의): 현 검색/라벨 용도에는 평면 추출(`function_declaration` 일괄)로 충분. 의미 구분(확장 함수 리시버/companion/top-level 태깅)은 2차 함수 호출그래프 고도화와 함께 진행.
- [x] **렌더링 라이브러리** — **[결정 D14]** Cytoscape.js(MIT).
- [x] **초기 뷰 / 클릭 홉** — **[결정 D15/D16]** 대규모는 진입점 중심+점진 확장(경계 **2000 확정**, `03 §8`), 클릭 1-홉+확장. 목표 FPS도 확정(경계에서 ≥30fps).
- [x] **앱 UI 표시 언어** — **[결정 D12]** 한국어 유지.

---

> 갱신 규칙: 새 한계가 생기면 해당 카테고리에 추가하고 [발생] 작업 단위를 적는다. 해결되면 `[x]`로 체크하고 처리 PR/커밋을 한 줄 남긴다.
