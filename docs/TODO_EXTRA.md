# TODO_EXTRA — 한계 · 미해결 · 기술부채 모음

> 구현 중 발견한 **한계점 · 임시 결정 · 미뤄둔 문제**를 모은다. **모든 MVP 작업이 끝난 뒤 다시 훑어볼 목록.**
> - 정식 기능 투두 → `docs/TODO.md`
> - SPEC 단계의 미해결/제안 → 각 SPEC 문서 하단 `추가)`
> - 본 문서는 그 사이의 "구현하다 생긴 빚/한계"를 추적한다.
>
> 각 항목 표기: **[현황]** 무엇이 부족한가 · **[영향]** 무엇이 위험한가 · **[대응]** 예정 마일스톤 또는 `미정`. (발생: 작업 단위)

---

## A. 보안 / 배포

- [x] **렌더러 CSP 완화** (발생: M0) — **해결**(#62): 정적 `<meta>` CSP 제거, main에서 응답 헤더로 dev/prod 분기 주입(`src/main/security.ts`). prod는 `script-src 'self'`로 `unsafe-inline`/`unsafe-eval` 제거. prod 빌드에서 인라인 스크립트 차단 양성 검증, Monaco/cytoscape/로그뷰 위반 0건.

- [x] **패키징 wasm 위치** (발생: M3_2/M3_3) — **M9 해결**: `electron-builder.yml` `extraResources`로 wasm 5종(tree-sitter + java/kotlin/c/cpp)을 asar 밖 `resources/wasm/`에 복사, 런타임 `wasm-paths.ts`가 패키징 경로를 해석.

- [ ] **앱 아이콘 미설정** (발생: M0) — 패키징 세부 중 **아이콘만 잔여**.
  - [현황] deb `maintainer`·자동 업데이트는 M9에서 반영됨. 단 `electron-builder.yml`에 `icon` 미지정, `resources/`(buildResources) 부재 → 기본 Electron 아이콘으로 패키징됨.
  - [영향] 배포본 브랜딩/식별성 저하(기능 무관).
  - [대응] 아이콘 자원(png/ico/icns) 추가 + `icon` 경로 지정. → `미정`.

- [ ] **`package.json`의 `allowScripts` 필드** (발생: M0)
  - [현황] 이 샌드박스 npm 래퍼 전용 필드(electron/esbuild 설치 스크립트 승인).
  - [영향] 표준 npm/CI는 무시(무해)하나 저장소에 환경 특화 흔적이 남음.
  - [대응] CI/표준 환경 정비 시 제거 검토. → `미정`.

## B. 파싱 / 분석 정확성

- [ ] **tree-sitter-kotlin 0.3.1 오탐** (발생: M3_2)
  - [현황] `package` + `class` 조합 등 일부 유효 Kotlin 코드에서 ERROR 노드 발생. 실제 안드로이드 Kotlin 파일은 package 선언이 흔함.
  - [영향] `hasError()`가 자주 true. 추출 정확도/신뢰 표시에 영향.
  - [대응] **M4 추출은 `hasError` 여부와 무관하게 노드 쿼리로 수행**(필수). 더 나은 Kotlin 문법/버전(별도 wasm 빌드) 검토. → `미정`.

- [ ] **web-tree-sitter 버전 고정(0.20.8)** (발생: M3_2)
  - [현황] 문법 wasm(`tree-sitter-wasms` 0.1.13, CLI 0.20.x)과 ABI를 맞추려 런타임을 0.20.8로 고정.
  - [영향] 런타임만 업그레이드하면 `Language.load` 실패. 최신 문법/기능 수용 어려움(기술부채).
  - [대응] 문법 wasm을 자체 빌드(최신 CLI)하거나 동반 업그레이드 경로 마련. → `미정`. (메모리 `tree-sitter-wasm-versions`)

- [ ] **`ANALYZER_VERSION` 수동 관리** (발생: M3_4)
  - [현황] 분석 로직이 바뀌면 `cache.ts`의 `ANALYZER_VERSION`을 수동으로 올려야 기존 캐시 무효화됨.
  - [영향] 깜빡하면 옛 캐시 재사용으로 잘못된 결과.
  - [대응] **M4에서 추출 추가 시 버전 상향 필수.** 자동화(해시 기반) 검토. → M4 진행 중 유의.

- [ ] **캐시 무효화 키 정밀도(stat 기반)** (발생: M3_4)
  - [현황] mtime+size 지문 사용. mtime 정밀도가 낮은 FS에서 같은 mtime·동일 size 변경은 미감지 가능.
  - [영향] 드물게 변경 미반영.
  - [대응] 정확성 필요 시 내용 해시 옵션 제공. → `미정`.

## C. 성능 / 확장성

- [ ] **분석이 워커 스레드 아님** (발생: M3_3) — **[결정] 메인+양보 유지(D11)**, 워커는 추후 최적화로 수용.
  - [현황] 메인 프로세스에서 파일 단위 협력적 양보(`setImmediate`)로 비차단. 단일 대형 파일 파싱 중에는 그 호출 동안 블록.
  - [영향] 초대형 파일/프로젝트에서 일시적 응답성 저하 가능.
  - [대응] 진짜 워커 스레드/`utilityProcess` 분리. → 대규모 성능 최적화(추후).

- [ ] **파스 트리 메모리 보관** (발생: M3_3)
  - [현황] 현재는 트리를 폐기(요약만). M4에서 추출 시 트리를 다수 보관하면 대규모에서 메모리 부담.
  - [영향] 대규모 프로젝트 메모리 사용량.
  - [대응] M4에서 파싱→추출→즉시 해제(스트리밍) 설계.

- [ ] **캐시 디렉터리 정리 정책 부재** (발생: M3_4)
  - [현황] 프로젝트별 1개 캐시 파일(덮어쓰기). 만료/상한/오래된 항목 정리 없음.
  - [영향] userData에 캐시 누적.
  - [대응] 상한/만료/LRU 정리. → `미정`.

- [ ] **대규모 렌더링 성능 합격선 미정** (발생: 계획)
  - [현황] 노드 수/FPS 합격선 미확정(`03 §8`, 추가-2).
  - [대응] → **M5** 착수 전 확정.

- [ ] **로그 디스크 스트리밍 미구현** (발생: M11_2)
  - [현황] M11_2는 **가상 스크롤**(보이는 라인만 렌더)로 대용량 열람을 해결. 단, 파일은 메인이 `readFile`로 전체를 읽어 렌더러가 `string[]`로 보관(전량 메모리 적재).
  - [영향] 초대형 로그(수백 MB+)에서 메모리 사용량 큼.
  - [대응] 디스크 라인-윈도우 스트리밍/인덱스 기반 부분 로드. → `미정`(추후).

- [ ] **Monaco 번들 비대** (발생: M12_1)
  - [현황] `monaco-editor` 전체를 import → 렌더 코어 청크 ~8MB(+언어 청크 코드분할). 워커는 editor.worker만 사용.
  - [영향] 렌더 번들 크기/초기 로드 증가(데스크톱이라 치명적이진 않음).
  - [대응] `editor.api` + java/kotlin basic-languages만 선택 import로 슬림화. → `미정`.

- [ ] **로그 줄바꿈(wrap) 토글 제거** (발생: M11_2)
  - [현황] 가변 행 높이가 고정 행높이 가상 스크롤과 충돌해 M11_1의 wrap 토글을 제거(가로 스크롤로 대체).
  - [영향] 긴 라인은 가로 스크롤로만 열람.
  - [대응] 측정-기반(가변 높이) 가상 스크롤 도입 시 wrap 복원. → `미정`.

## D. UX 미구현 / 약식

- [ ] **닫은 탭 복원(최근 닫은 탭)** (발생: M2) — `01` 추가 앱-1. [대응] `미정`.
- [ ] **동일 프로젝트 중복 탭 허용 여부** (발생: M2) — 앱-2. [대응] `미정`.
- [ ] **단축키 표 확정** (발생: M1) — **[결정 D13]** 수정자 키 Ctrl(Win/Linux 대상). 현재 Ctrl+O/T/W. 전체 표는 추후. 앱-3.
- [ ] **경로 깨진(이동/삭제) 탭 처리** (발생: M2) — 앱-5. [대응] `미정`.
- [x] **창/탭/파일/뷰 모드 세션 저장·복원** (발생: M1/M2) — **M8 해결**(저장소·창/탭/뷰 복원·손상 대비).
- [x] **뷰 모드 토글 UI** (발생: M2_4) — **M5/M6 해결**(관계도↔트리 전환 UI).
- [ ] **스캔 제외 규칙 설정 UI** (발생: M3_1) — 테스트 포함이 기본, 코드 상수로만 조정 가능. [대응] `미정`(설정 화면 도입 시).

## E. 검증 / 테스트 인프라

- [ ] **네이티브 다이얼로그/메뉴 E2E 미검증** (발생: M1_3/M2/M3_3)
  - [현황] `capturePage`는 네이티브 메뉴바/창 프레임/폴더 다이얼로그를 캡처/조작 불가. 실제 "메뉴 클릭→다이얼로그→분석" 라운드트립은 자동 검증 안 됨(단위 테스트+시드 캡처+코드리뷰로 대체).
  - [영향] IPC 통합 경로의 회귀를 자동으로 못 잡음.
  - [대응] Playwright(Electron) 등 E2E 도입 검토. → `미정`.

- [x] **CI 부재** (발생: 전반) — **M9 해결**: `.github/workflows/build.yml`(Linux/Windows 매트릭스). PR/푸시는 typecheck+lint+test+build, 태그(`v*`)는 멀티 OS 패키징 + GitHub Releases 퍼블리시(`--publish always`).

- [ ] **캡처 모드 데모 시드 코드** (발생: M2~M3)
  - [현황] `main.ts`에 `captureMode`용 데모 탭/분석 결과 시드 코드가 포함.
  - [영향] 검수 전용 코드가 렌더러 번들에 상존.
  - [대응] 별도 dev 전용 경로로 분리 또는 빌드시 제거 검토. → `미정`.

## F. 미결정 SPEC 항목(단계별 결정 예정 — 참고용)

- [x] **영역(Domain) 분류 방식** — **[결정 D10]** 특정 레이아웃 미가정, 모듈/최상위 디렉터리 기반 자동 그룹 + 사용자 매핑(범용). 구체 그룹/색 규칙은 M4_5.
- [x] **파일 의존성 정의 범위** — **[결정 D9]** MVP는 import 기반, 프로젝트 밖 import는 `external` 노드(토글). 동일 패키지/리소스는 MVP 범위 밖.
- [ ] **Kotlin 특유 요소(확장 함수/최상위/컴패니언) 처리** — `02` 추가-3. [대응] M4.
- [x] **렌더링 라이브러리** — **[결정 D14]** Cytoscape.js(MIT).
- [x] **초기 뷰 / 클릭 홉** — **[결정 D15/D16]** 대규모는 진입점 중심+점진 확장(경계 잠정 2000), 클릭 1-홉+확장. 진입점 선정 규칙/목표 FPS는 M5/M6에서 측정.
- [x] **앱 UI 표시 언어** — **[결정 D12]** 한국어 유지.

---

> 갱신 규칙: 새 한계가 생기면 해당 카테고리에 추가하고 [발생] 작업 단위를 적는다. 해결되면 `[x]`로 체크하고 처리 PR/커밋을 한 줄 남긴다.
