# TODO — 구현 투두리스트

> **구현 진행의 기준 문서.** 체크박스 1개 = 작업 1단위(워크플로: CLAUDE.md §6).
> 위에서부터 **순차적으로** 진행하고 임의로 건너뛰지 않는다. (CLAUDE.md §5)
> 각 항목은 관련 SPEC을 `파일 §섹션`으로 참조한다. 구현 직전 SPEC을 먼저 읽는다. (CLAUDE.md §6.1)
> 리뷰 요청/결과 파일명은 마일스톤에 대응한다. 예: M1 세 번째 → `M1_3.md`.
>
> **묶음 원칙**: 각 마일스톤(M*)은 **하나의 공통 주제**만 담는다. 영속성·배포처럼 여러 기능에 걸친 주제는 별도 마일스톤으로 모은다.
> **단계 게이트**: `(2차)`/`(추후)` 마일스톤은 착수 전 사용자와 단계를 합의한다. (`00` §8)

범례: `[ ]` 미완료 · `[x]` 완료 · `(MVP/2차/추후)` 단계 (`00` §8.1)

---

# MVP (1차)

## M0 — 스캐폴딩 / 툴체인 (MVP)

- [x] Electron + TypeScript + electron-vite 초기화, `npm run dev` HMR 기동 (`00` §4, `01` §2, CLAUDE.md §9)
- [x] main/preload/renderer 프로세스 골격 + 보안 기본값(contextIsolation on, nodeIntegration off, preload 경유 IPC) (`01` §2)
- [x] typecheck / lint / format 스크립트 구성 (CLAUDE.md §9)
- [x] Vitest 설정 + 스코프 테스트 1개 (CLAUDE.md §9)
- [x] `.gitignore` 구성(`screenshots/`, `reviews/`, `dist/`, `out/`, `node_modules/`) (CLAUDE.md §6, §9)
- [x] UI 검수용 스크린샷 캡처 절차(헤드리스 `xvfb` 지원) (CLAUDE.md §6)

## M1 — 앱 셸: 창 / 단일 인스턴스 (MVP)

- [x] 메인 윈도우 생성 + 최소화 / 최대화 / 종료 (`01` §3)
- [x] 단일 인스턴스 락 + 두 번째 실행 시 기존 창 포커스 (`01` §7)
- [x] 기본 메뉴(프로젝트 열기 / 새 탭 / 탭 닫기 / 종료) (`01` §8)

## M2 — 프로젝트 선택 / 다중 탭 (MVP)

- [x] 폴더 선택 다이얼로그 → 새 탭에 프로젝트 로드 (`01` §4)
- [x] 탭 추가 / 닫기 / 전환 (`01` §4)
- [x] 빈 상태(프로젝트 미선택) 탭/화면 (`01` §4)
- [x] 탭별 독립 상태 컨테이너(프로젝트·분석결과·뷰상태 분리) (`01` §4)

## M3 — 코드 분석: 스캔 / 파싱 기반 (MVP)

- [x] 프로젝트 재귀 스캔 + 대상 확장자 수집 + 제외 규칙(빌드/생성물) (`02` §3, 추가-4)
- [x] Tree-sitter(java/kotlin) 통합 + 파일 파싱(실패 파일 건너뜀·기록) (`02` §2, §3, §8)
- [x] 분석 실행 구조(워커/진행률·완료 보고, 비차단) (`02` §8, 추가-6)
- [x] 분석 캐시(무효화 키) + 재사용 (`02` §7.2)

## M4 — 코드 분석: 관계 / 심볼 / 영역 (MVP)

- [x] 노드/엣지 데이터 모델 정의(`file`/`function`, `file-dependency`) (`02` §4)
- [x] 파일 의존성 추출(import 기반) → 엣지 산출(부모=의존하는 쪽) (`02` §3, §4.3)
- [x] 심볼 해석 + 외부/미해결 노드 분리(오탐 방지) (`02` §7.1, §9)
- [ ] 함수/메서드 **정의** 추출(검색·라벨용, 호출 엣지 제외) (`02` §1, §4.1)
- [ ] 영역(Domain) 분류: 사용자 매핑 + 안드로이드 프리셋(프리셋 패턴 사전 확정) (`02` §5)

## M5 — 시각화: 렌더 기반 / 뷰 (MVP)

- [ ] 렌더링 라이브러리 선택 확정(Cytoscape.js vs sigma.js) (`03` §7, 추가-1)
- [ ] 그래프 캔버스 기본 렌더 + 팬 / 줌 / 드래그 (`03` §2, §5.3)
- [ ] 관계도(태양계형) 뷰 — 방사형 배치 (`03` §3)
- [ ] 트리 뷰 + 접기/펼치기 (`03` §4)
- [ ] 초기 뷰(로드 직후 표시 정책: 소규모 전체 / 대규모 진입점 중심) (`03` §5.1)
- [ ] 대규모 표시 전략(초기 N-홉 / 점진 확장; 성능 합격선은 사전 확정) (`03` §8, 추가-2)

## M6 — 시각화: 인터랙션 / 색상 (MVP)

- [ ] 관계도 ↔ 트리 전환(선택/포커스 유지) (`03` §5.2)
- [ ] 파일 클릭 → 부모/자식 표시(1-홉 + 확장) (`02` §4.3, `03` §5.3)
- [ ] 선택 노드/경로 하이라이트 + 나머지 디엠퍼사이즈 (`03` §9)
- [ ] 영역별 색상 + 범례(legend) (`03` §6)
- [ ] 노드 정보 패널(경로/심볼/영역/라인) — 읽기 전용(편집기 연동은 M12) (`03` §10)

## M7 — 검색: 파일 / 함수명 (MVP)

- [ ] 노드 이름 인덱스 구축(파일명 + 함수 정의명) (`05` §5)
- [ ] 파일/함수명 검색(부분 일치 + 옵션: 대소문자/정확/경로) (`05` §2, §3)
- [ ] 퍼지 검색 (`05` §4)
- [ ] 결과 선택 → 그래프 포커스 동기화 (`05` §2, `03` §10)
- [ ] 검색 히스토리 (`05` §7)

## M8 — 세션 / 상태 영속성 (MVP)

> 기능들이 구현된 뒤(M1·M2·M5·M6) 그 상태를 일괄 영속화한다.

- [ ] 저장소: 저장 위치(userData) + 원자적 쓰기 기반 (`01` §6, §10)
- [ ] 창 상태(크기/위치/최대화, 멀티모니터 보정) 저장·복원 (`01` §3, §5)
- [ ] 탭 / 프로젝트 경로 / 활성 탭 저장·복원 (`01` §5)
- [ ] 열린 파일(선택/포커스 노드) · 뷰 모드 저장·복원 (`01` §5, `03` §5.2)
- [ ] 세션 손상 대비(백업·폴백·안전 기동·비차단 알림) (`01` §10)

## M9 — 배포 파이프라인 (MVP 마감)

- [ ] `docs/DEPLOY.md` 작성(빌드/배포/업데이트 상세) (CLAUDE.md §9, `01` §9)
- [ ] Linux 빌드(AppImage + deb) (CLAUDE.md §9)
- [ ] Windows 빌드(포터블 zip / nsis) (CLAUDE.md §9)
- [ ] 자동 업데이트 설계 반영 (`01` §9)

---

# 2차 (사전 합의 후 진행)

## M10 — 함수 호출 그래프 (2차)

- [ ] 함수 호출 추출 + `function-call` 엣지 구축 (`02` §6)
- [ ] 함수 클릭 → 호출처 역추적 표시(함수 = 마지막 노드) (`02` §6, `03` §5.3)

## M11 — 로그 덤프 분석 (2차)

- [ ] 로그 덤프 열람 영역 (`04` §2)
- [ ] 대용량 처리(스트리밍 로드 / 가상 스크롤) (`04` §3)
- [ ] 로그 필터링(레벨/태그/텍스트/정규식) (`04` §4)
- [ ] 로그 → 코드 역추적(매칭 + 다중 후보 처리) (`04` §5)
- [ ] 3-뷰 레이아웃 + 3중 연동(로그 ↔ 노드 ↔ 코드) (`04` §6, §7, `03` §10)
- [ ] 로그 검색 (`04` §8, `05` §6)

## M12 — 앱 내 코드 편집 (2차)

- [ ] Monaco 편집기 통합 + 노드 → 소스 열기/이동 (`06` §2, `03` §10)
- [ ] 저장 + 편집 안전장치(외부 변경 감지 / 미저장 경고 / 원자적 쓰기) (`06` §3, §6)
- [ ] 저장 시 증분 재분석 + 그래프/검색 인덱스 갱신 (`06` §4, `02` §3)
- [ ] diff 하이라이트 + 영향 범위 표시 (`06` §5)

## M13 — C/C++ 네이티브 분석 (2차)

- [ ] tree-sitter c/cpp 통합 + include 기반 파일 의존성 (`02` §2, §3)

---

# 추후 (사전 합의 후 진행)

## M14 — 확장 기능 (추후)

- [ ] JNI 경계 분석 고도화 (`02` §2)
- [ ] 로그 매칭 신뢰도 표시 (`04` §5.3)
- [ ] 그래프 전/후 비교 모드 (`03`, `06` §5)
- [ ] 라이트 모드 (`03` §9)

---

> 각 체크박스 구현 직전, 성공 기준을 사용자와 확정한다. (`00` §9, CLAUDE.md §4)
> 착수 전 결정이 필요한 `추가)` 미해결 항목: 안드로이드 영역 프리셋 패턴(`02` 추가-1), 렌더링 라이브러리(`03` 추가-1),
> 대규모 성능 합격선·초기 뷰 기본값·클릭 표시 홉 수(`03` 추가-2,7,8), 스캔 제외 규칙(`02` 추가-4).
