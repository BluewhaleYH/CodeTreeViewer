# TODO_MORE — 사용자 요청 이슈 · 수정 · 신규 기능

> 사용자가 구두/채팅으로 요청한 항목을 기록하고 구현 워크플로(CLAUDE.md §6)로 처리한다.
> `[ ]` 미완료 · `[x]` 완료(처리 PR 표기) · 🚧 보류(사유).

---

## UX 개선 (그래프/레이아웃) — 2026-06-22 요청

- [x] **그래프 줌 속도 상향** (#79 → #83) — `wheelSensitivity` 0.2 → 0.5 → **1.0**(cytoscape 기본값, 원본의 5×). 그래프 위 **그냥 휠**(Ctrl 없이)이 그래프 줌. Ctrl+휠은 Electron 페이지 줌(별개).
- [x] **우상단 팝업 겹침** (#79) — 우측 정보 패널(정보/후보/영향/비교/역추적)을 `.overlay-right` 세로 스택으로 모아 겹침 해소.
- [x] **코드 뷰 너비 리사이즈** (#79) — 코드 패널 좌측 경계에 `.ws-resizer` 스플리터(pointer 드래그, 280px~70% clamp). 패널과 함께 표시/숨김.

## 도구 / 테스트 — 2026-06-22 요청

- [x] **릴리스 없이 테스트 빌드** (#80) — `.github/workflows/test-build.yml`(workflow_dispatch). 수동 실행 시 Windows 포터블 zip을 Actions 아티팩트로 업로드. (#82에서 프리릴리스 방식으로 교체)
- [x] **로그인 없이 받는 테스트 빌드** (#82) — Actions 아티팩트는 로그인 필요 → 고정 태그 `test-build`의 **프리릴리스**(nsis exe + 포터블 zip)로 발행하도록 교체. 공개 다운로드 가능, `latest` 아님(자동 업데이트 영향 없음), 매 빌드마다 게시물 갱신.

## 관계도/분석 품질 — 2026-06-22 요청

- [x] **노드 색상 가짓수 축소** (#84) — 영역 팔레트 10색 → 5색(초과 도메인 순환).
- [x] **상속 관계 추출** (#84) — Java extends/implements, Kotlin 상위타입, C++ base → `inheritance` 엣지. 같은 패키지는 단순명으로 해석(모호 시 미연결). 흰 삼각형 화살표.
- [x] **교차 파일 호출 집계** (#84) — 함수 호출이 다른 파일이면 `file-call` 파일 엣지로 집계(메인 관계도 표시). 역추적 모드는 유지.
- [x] **레이아웃 개선(방향성 계층)** (#85) — 관계도를 cytoscape-dagre(TB)로 교체 → 부모(의존하는 쪽) 위, 자식 아래로 흐름. 트리는 breadthfirst 유지. 격리 캡처로 의존 방향·전 엣지 종류 확인.

## 그래프 인터랙션/코드 연동 — 2026-06-22 요청 (2차)

- [x] **역추적 초기 depth 확대** (#86) — `callersUpToDepth`로 초기 6단계까지 호출처 표시(사이클 차단). 이후 노드 클릭으로 더 확장.
- [x] **함수 노드에 소속 파일 표시** (#86) — 함수 노드 라벨을 "함수명\n파일명" 2줄로.
- [x] **라벨 겹침/깨짐 수정** (#86) — 노드 라벨에 어두운 반투명 배경 + 줄바꿈/최대폭, 레이아웃 간격 확대(dagre nodeSep 50, breadthfirst spacingFactor↑).
- [x] **코드 패널 토글 버튼** (#86) — 우상단 `</>` 버튼으로 코드 패널 켜고/끄기(활성 시 강조).
- [x] **노드 클릭 → 코드 패널 표시** (#86) — 파일/함수 노드 클릭 시 해당 소스를 코드 패널에 자동 표시(함수는 해당 라인).
- [x] **노드 클릭 시 80% 맞춤** (#86) — 클릭한 노드+이웃을 화면의 ~80%로 fit(가장자리 여백).

## 그래프 인터랙션 3차 — 2026-06-24 요청

- [x] **역추적 리프노드 = 선택 노드만** (#87) — `backtraceTree`: 선택 노드(root)로부터 depth 계산 후 root 쪽으로 흐르는 엣지(depth(callee)<depth(caller))만 유지 → 교차 엣지 제거, root만 유일 sink. 단위 테스트.
- [x] **기본 줌: 선택 없으면 최상위 노드, 있으면 선택 노드로** (#87) — draw/역추적 시 `pickFocus`(선택 or incoming 없는 루트)로 fit(~80%). 캡처 확인.
- [x] **탭 전환 멈춤 버그** (#87, 성능 후속) — 원인은 **대규모 그래프**를 탭 전환마다 dagre 레이아웃+캔버스 렌더(동기)로 다시 그려 UI가 막히는 것(예외가 아님 → 데모로 재현 불가했던 이유). 초기 렌더 노드 상한 2000→600 + 검색 인덱스 캐시로 완화. 상세는 "성능" 섹션. `graphView.sync` try/catch 가드는 유지.
- [x] **코드 에디터에서 함수명 선택 → 노드 선택** (#87) — Monaco 식별자 선택 시 현재 파일 함수(없으면 전역 유일)로 역추적 진입.

## 빌드 식별 / 신뢰성 — 2026-06-24 요청 (반영 안 됨 신고 대응)

- [x] **빌드마다 고유 파일명 + 타이틀바 빌드 ID** — "수정사항 반영 안 됨" 신고는 동일 파일명(`0.3.0`)으로 **옛 빌드가 실행**된 캐시/구버전 혼동이 원인. (1) `electron.vite.config.ts` `buildId()`(짧은 커밋 해시)를 `__BUILD_ID__`로 주입 → 앱 **타이틀바에 `· <sha>`** 표시. (2) 테스트빌드 산출물 파일명에 `$SHA` 부착(`CodeTreeViewer-<sha>-win.zip`/`...-Setup-<sha>.exe`), 릴리스 안내에 새 폴더 압축 해제·타이틀바 확인 명시.
- [x] **레이아웃 완료 후 줌 보장** — 기본 줌(#87)이 큰 그래프에서 비동기 레이아웃에 덮여 전체로 보일 가능성 차단. `runLayoutThenFocus`로 레이아웃을 명시 실행하고 `layoutstop` 이후에만 포커스 노드로 fit(메인 그래프 + 역추적 모드). 무선택 첫 시작이 최상위 노드 영역으로 줌됨을 격리 캡처로 확인.
- [x] **함수명 단일 클릭에도 노드 선택** — `onDidChangeCursorSelection`에서 선택 텍스트가 비면 `getWordAtPosition`으로 커서 위치 단어를 사용 → 드래그 선택뿐 아니라 단일 클릭으로도 동작.

## 그래프 인터랙션 4차 — 2026-06-24 요청

- [x] **파일 노드도 역추적 진입(depth 적용)** — 역추적을 엣지 종류로 일반화(`buildCallerAdjacency`/`backtraceTree`에 `edgeTypes`). 함수=`function-call`, 파일=`file-dependency`+`file-call`. 파일 정보 패널에 **"이 파일 역추적"** 버튼 → 그 파일을 의존/호출하는 쪽을 depth 6까지 거슬러 표시(노드 클릭으로 더 확장). 단위 테스트.
- [x] **첫 시작 = 전체 그래프 + ingoing 최다 노드 강조** — 무선택 초기 포커스를 "최상위(incoming 0) 노드 줌"에서 **전체 그래프 fit + ingoing이 가장 많은 노드 강조**로 변경(`applyInitialFocus`/`mostIncomingId`). 선택이 있으면 기존대로 그 노드로 줌.
- [x] **노드 클릭 → 그 노드 코드 표시(함수는 정의 라인)** — 메인 그래프 클릭은 기존 `onGraphSelect`로 소스 오픈. 역추적 모드 노드 클릭은 선택/모드를 바꾸지 않는 `onOpenSource` 콜백으로 소스 오픈(함수=정의 라인, 파일=첫 줄). `store.setSelectedNode`가 역추적을 종료하는 문제를 피하려 별도 경로 사용.
- [x] **클릭 시 in/out 강조·나머지 흐리게** — 단일 클릭은 `applySelectedStyle`(선택+직접 이웃=closedNeighborhood 강조, 나머지 `.dimmed`)만 적용하고 **줌은 그대로 둬서** 흐려진 모습이 보이게. ~80% fit은 더블클릭으로 이동. 증분 확장 레이아웃은 `fit:false`로 뷰포트 보존. 격리 캡처로 확인.
- [x] **현재 보기 내 검색(중첩)** — 검색에 **"보기 내"** 토글 추가. 켜면 `GraphView.getDisplayedIds()` 범위로 결과 한정(함수는 소속 파일 노드 또는 함수 노드 자체가 보일 때 포함). 끄면 기존 프로젝트 전역 검색.
- [x] **함수 리스트 전체 표시** — 정보 패널 함수 목록의 30개 컷(`…외 N개`) 제거, 전부 표시하되 길면 패널 내부 스크롤(`max-height`).

## 성능 — 2026-06-24 요청 (대규모 프로젝트 ~3600 파일)

- [x] **분석 시 파일 재읽기 제거** — 신규 분석(캐시 미스) 때 전체 파일을 **두 번 읽던**(`contentFingerprint` + 파싱) 것을 1회로. 파싱하며 같은 바이트로 내용 지문을 누적해 `cache.contentFingerprint`로 재사용(`analyzeScanned`가 반환). 지문 포맷 동일성(하이브리드 캐시 체크 유지)을 단위 테스트로 보장.
- [x] **읽기 선행(read-ahead)으로 I/O·파싱 겹치기** — 파서는 단일(web-tree-sitter)이라 파싱은 순차 유지하되, 최대 24개 파일을 미리 읽어 디스크 I/O 지연을 파싱 CPU 뒤로 숨김(`READ_AHEAD`).
- [x] **지문 stat 병렬화** — `fileFingerprint`의 순차 `stat`을 동시성 64로 병렬화(순서 보존 `mapLimit`) → 대규모에서 캐시 적중 판단 가속.
- [x] **탭 전환 멈춤 완화(#39)** — 원인은 큰 그래프를 탭 전환마다 dagre 레이아웃+캔버스 렌더(동기)로 다시 그리는 것. 초기 렌더 노드 상한을 2000→**600**으로 낮춰 진입점 중심 일부만 먼저 그리고 나머지는 클릭 확장(스펙 D15). 검색 인덱스도 그래프 참조가 같으면 매 렌더 재구성하지 않게 캐시.

## 언어/인터랙션 5차 — 2026-06-24 요청

- [x] **C/C++ 함수 추출(함수 클릭·노드 이동·코드 이동)** — `extractCFamily`가 `functions: []`였음(함수 분석 미구현). `function_definition`에서 함수명(`cppDeclName`: 포인터/참조/`Class::method` 처리) + 본문 `call_expression` 호출(`cppCalleeName`)을 추출 → 함수 노드/`function-call` 엣지 생성. **사용자 요청으로 C/C++ 함수 단계 확장.** 단위 테스트 추가. (Kotlin은 이미 정상 — 실파싱 프로브로 함수/호출 엣지 생성 확인. 미작동 신고는 옛 빌드/줌 문제로 추정.)
- [x] **역추적 = 체인 전체 보이기(자매 노드 확인)** — 역추적 진입 시 선택 노드만 줌(`fitToSelection(root)`)해서 위쪽 호출처 체인이 화면 밖으로 잘려 "자매 노드 없음"을 확인하기 어려웠음. `backtraceLayout`에 `fit:true`를 주고 leaf-줌 오버라이드 제거 → **체인 전체**가 보임. (자매 노드 제외 로직은 이미 정상 — D→B→A에서 B의 다른 자식 C가 표시되지 않음을 격리 캡처로 재확인.)
- [x] **클릭 시 흐림 강조 가시성↑** — `.dimmed` opacity 0.16→**0.08**로 더 또렷하게(선택 노드의 in/out 이웃만 강조, 나머지 흐림). 단일 클릭은 줌을 바꾸지 않아 흐림이 그대로 보임(기존 로직 유지).
- [x] **중첩 검색을 별도 영역으로 분리** — 메인 검색의 "보기 내" 체크박스 제거. 대신 **우상단에 별도 "보기 내 검색" 박스**(점선 강조 테두리, `SearchView` 2번째 인스턴스, 항상 현재 표시 노드로 한정). 메인 검색은 프로젝트 전역.

## JNI 고도화 — 2026-06-24 (AOSP Bluetooth 실측으로 발견)

- [x] **RegisterNatives(JNINativeMethod) 방식 JNI 경계 탐지** — 실제 AOSP Bluetooth로 분석해보니 JNI 경계 엣지가 **0개**였음. 원인: 기존 탐지(M14_1)는 `Java_...` 맹글링 함수명 규칙만 매칭하는데, **AOSP는 이 규칙을 안 쓰고**(BT에 `Java_` 함수 0개) `JNINativeMethod` 테이블 + `REGISTER_NATIVE_METHODS(env, "com/android/.../Foo", methods)` 방식을 씀(BT 20개 파일). → C/C++ 소스의 **클래스 디스크립터 문자열**("com/android/.../Foo")을 FQN으로 수집(`extractJniClassRefs`)해 프로젝트 Java 클래스(`typeIndex`)와 매칭 → `jni-boundary` 엣지(Java 인터페이스 → C++ 등록 파일). **CLAUDE.md "JNI 경계 고도화(추후)" 단계를 사용자 동의로 진행.** 실측 결과 BT 모듈에서 JNI 엣지 0→**42개**(`A2dpSinkNativeInterface.java → com_android_bluetooth_a2dp_sink.cpp` 등 정확). 합성 단위 테스트 추가. (기존 `Java_` 맹글링 매칭은 유지.)
